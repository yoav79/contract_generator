# PostgreSQL local — reemplaza USER y PASSWORD con tus credenciales.
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/contract_generator?schema=public"

# Secreto para firmar cookies de sesión — genera uno aleatorio de al menos 32 caracteres.
SESSION_SECRET="replace-with-a-random-secret-at-least-32-characters-long"
